/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://cornrichsilage.in/',
    generateRobotsTxt: true, // also generates robots.txt
    changefreq: 'daily',
    priority: 0.7,
    outDir: 'public',
  }